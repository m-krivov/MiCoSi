
#include "vec3x.h"

//-------------
//--- vec3f ---
//-------------

const vec3f vec3f::ZERO = vec3f(0.0f, 0.0f, 0.0f);
const vec3f vec3f::DEFAULT_DIRECT = vec3f(0.0f, 0.0f, -1.0f);
const vec3f vec3f::DEFAULT_UP = vec3f(0.0f, -1.0f, 0.0f);
const vec3f vec3f::DEFAULT_LEFT = vec3f(-1.0f, 0.0f, 0.0f);

//-------------
//--- vec3d ---
//-------------

const vec3d vec3d::ZERO  = vec3d(0.0, 0.0, 0.0);
const vec3d vec3d::DEFAULT_DIRECT = vec3d(0.0, 0.0, -1.0);
const vec3d vec3d::DEFAULT_UP = vec3d(0.0, -1.0, 0.0);
const vec3d vec3d::DEFAULT_LEFT = vec3d(-1.0, 0.0, 0.0);
